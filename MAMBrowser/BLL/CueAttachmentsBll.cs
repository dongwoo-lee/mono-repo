using Dapper;
using M30.AudioFile.DAL;
using System;
using System.Collections.Generic;
using System.IO;

namespace MAMBrowser.BLL
{
    public class CueAttachmentsBll
    {
        //private readonly DALFactory _factory;
        //public CueAttachmentsBll(DALFactory factory)
        //{
        //    _factory = factory;
        //}
        ////첨부파일 가져오기
        //public IEnumerable<AttachmentsDTO> GetFile(int[] cueids)
        //{
        //    return _factory.GetAdditionalDAL().GetAttachments(cueids);
        //}
        ////출력용 가져오기
        //public IEnumerable<PrintDTO> GetPrints(int[] cueids)
        //{
        //    return _factory.GetAdditionalDAL().GetPrints(cueids);
        //}
        ////태그 가져오기
        //public IEnumerable<TagDTO> GetTags(int[] cueids)
        //{
        //    return _factory.GetAdditionalDAL().GetTags(cueids);
        //}

    }
}
